// This component now wraps the new OnboardingFlow
// The old simple tutorial is replaced with the full onboarding experience
import { OnboardingFlow } from '@/components/Onboarding';

export function OnboardingModal() {
  return <OnboardingFlow />;
}
